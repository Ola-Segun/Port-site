<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" viewBox="0 0 100 100" preserveAspectRatio="xMidYMid slice">
    <rect width="100%" height="100%" fill="#cccccc"/>
    <text x="50%" y="50%" font-family="Arial, sans-serif" font-size="14" text-anchor="middle" dy=".3em" fill="#ffffff">
      Placeholder
    </text>
    <style>
      @media (max-width: 100px) {
        text {
          font-size: 10px;
        }
      }
      @media (max-width: 50px) {
        text {
          font-size: 6px;
        }
      }
    </style>
    <script type="text/javascript">
      <![CDATA[
        function updateSize() {
          var svg = document.querySelector('svg');
          var params = new URLSearchParams(window.location.search);
          var width = params.get('width');
          var height = params.get('height');
          if (width) svg.setAttribute('width', width + 'px');
          if (height) svg.setAttribute('height', height + 'px');
        }
        updateSize();
      ]]>
    </script>
  </svg>